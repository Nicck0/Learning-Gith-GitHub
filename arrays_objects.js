let team = [
  { name: "Alice", role: "Developer" },
  { name: "Bob", role: "Designer" },
  { name: "Charlie", role: "Manager" }
];

// Print each person's name and role
for (let i = 0; i < team.length; i++) {
  console.log(team[i].name + " is a " + team[i].role);
}